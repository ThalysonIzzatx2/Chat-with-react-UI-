import React from 'react';

// import { Container } from './styles';

export default function Header() {
  return (
    <header>
        <nav>
            <div>UI</div>
        </nav>
    </header>
  );
}
